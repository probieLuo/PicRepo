<Window
    x:Class="PicRepo.Client.Views.Settings.WinAddPicRepo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctl="clr-namespace:PicRepo.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PicRepo.Client.Views.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    Title="WinAddPicRepo"
    Width="300"
    prism:ViewModelLocator.AutoWireViewModel="True"
    AllowsTransparency="True"
    Background="Transparent"
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    mc:Ignorable="d">
    <Border
        Background="{DynamicResource AccentAcrylicBackgroundFillColorDefaultBrush}"
        BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="{DynamicResource OverlayCornerRadius}"
        Opacity="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock
                    Margin="5"
                    FontWeight="Bold"
                    Text="新建图床配置" />
                <TextBox
                    Margin="5"
                    ctl:WatermarkHelper.Watermark="图床配置名"
                    Text="{Binding NewPicRepoName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Margin="5"
                    Command="{Binding AddPicRepoCommand}"
                    Content="确定"
                    IsDefault="True" />
                <Button
                    Margin="5"
                    Content="取消"
                    IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>