<UserControl
    x:Class="PicRepo.Client.Views.Settings.UcGeneralSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PicRepo.Client.Views.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Margin="5"
            VerticalAlignment="Center"
            Text="主题模式" />
        <ComboBox
            Grid.Row="0"
            Grid.Column="1"
            Width="150"
            Margin="5"
            HorizontalAlignment="Right"
            DisplayMemberPath="Name"
            ItemsSource="{Binding Themes}"
            SelectedValue="{Binding SelectedTheme}" />
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Margin="5"
            VerticalAlignment="Center"
            Text="关闭主窗口时最小化到系统托盘" />
        <CheckBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="5"
            HorizontalAlignment="Right"
            Content=""
            IsChecked="{Binding MinimizeToTrayOnClose}" />
        <TextBlock
            Grid.Row="2"
            Grid.Column="0"
            Margin="35,5,5,5"
            VerticalAlignment="Center"
            Text="- 启用Mini窗口"
            Visibility="{Binding MinimizeToTrayOnClose, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <CheckBox
            Grid.Row="2"
            Grid.Column="1"
            Margin="5"
            HorizontalAlignment="Right"
            Content=""
            IsChecked="{Binding MiniWindowingOnClose}"
            Visibility="{Binding MinimizeToTrayOnClose, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBlock
            Grid.Row="3"
            Grid.Column="0"
            Margin="5"
            VerticalAlignment="Center"
            Text="清除历史记录" />
        <Button
            Grid.Row="3"
            Grid.Column="1"
            Margin="5"
            HorizontalAlignment="Right"
            Command="{Binding ClearCacheCommand}"
            Content="清除" />
        <TextBlock
            Grid.Row="4"
            Grid.Column="0"
            Margin="5"
            VerticalAlignment="Center"
            Text="默认复制到粘贴板" />
        <ComboBox
            Grid.Row="4"
            Grid.Column="1"
            Width="150"
            Margin="5"
            HorizontalAlignment="Right"
            ItemsSource="{Binding CopyTypes}"
            SelectedValue="{Binding SelectedCopyType}" />
    </Grid>
</UserControl>