<UserControl
    x:Class="PicRepo.Client.Views.Settings.UcPicRepoSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:PicRepo.Client.Helper"
    xmlns:local="clr-namespace:PicRepo.Client.Views.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="GitHub">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Button Margin="0,0,0,5" Command="{Binding AddConfigCommand}">
                            <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE710;" />
                        </Button>
                        <ListBox
                            Grid.Row="1"
                            ItemsSource="{Binding PicRepoConfigs}"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            SelectedItem="{Binding SelectedConfig}">
                            <ListBox.ItemContainerStyle>
                                <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock
                                            Grid.Column="1"
                                            HorizontalAlignment="Right"
                                            Text="默认"
                                            Visibility="{Binding IsDefault, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                    <ScrollViewer
                        Grid.Column="1"
                        Margin="5"
                        VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,0,20,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock FontWeight="Bold" Text="配置项" />

                                <TextBlock
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    Text="{Binding SelectedGithubConfigItem.Name}"
                                    TextTrimming="CharacterEllipsis" />
                            </Grid>
                            <TextBlock Text="仓库名" />
                            <TextBox Text="{Binding SelectedConfig.Repo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="用户名" />
                            <TextBox Text="{Binding SelectedConfig.Owner, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="分支" />
                            <TextBox Text="{Binding SelectedConfig.Branch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="Token" />
                            <PasswordBox helper:PasswordHelper.BindPassword="True" helper:PasswordHelper.BoundPassword="{Binding SelectedConfig.Token, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <StackPanel
                                Margin="0,10,0,10"
                                HorizontalAlignment="Right"
                                Orientation="Horizontal">
                                <Button
                                    Margin="0,0,10,0"
                                    Command="{Binding DeleteConfigCommand}"
                                    Content="删除" />
                                <Button
                                    Margin="0,0,10,0"
                                    Command="{Binding SetDefaultCommand}"
                                    Content="默认" />
                                <Button
                                    Command="{Binding SaveConfigCommand}"
                                    Content="保存"
                                    IsDefault="True" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>